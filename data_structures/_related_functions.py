#this for strings, tuples, lists only
x=[2,4,'e',3];
print(x, x[0:2], x[2:9], x[3:], x[:2], x[-1], x[:-1]);    #poweful way to slice

print(len(x));

name='Shashika Pradeep';
print(name, name[0:2], name[2:9], name[3:], name[:2], name[-1], name[:-1]);    #poweful way to slice
