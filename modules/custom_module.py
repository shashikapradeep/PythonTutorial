# when running a  custom module __init__.py file should be defined. Then python understands that the folder is created as a python Module.
# then basic init configurations are running in __init__.py file as it runs first.


